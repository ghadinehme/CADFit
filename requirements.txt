# CADFit dependencies. Tested with Python 3.10.
# Versions are the ones we verified in the cadquery-env.
# numpy is left to resolve to the latest 2.x since cadquery's transitive
# dependency `nlopt>=2.9` requires numpy>=2.

cadquery==2.6.1
cadquery-ocp==7.8.1.1.post1
manifold3d==3.4.1
numpy>=2,<3
scipy>=1.13,<2
matplotlib>=3.7,<4
shapely>=2.0,<3
scikit-learn>=1.3,<2
trimesh==4.11.5
networkx>=3.0
rtree>=1.0
tqdm>=4.65
pymeshlab==2025.7
